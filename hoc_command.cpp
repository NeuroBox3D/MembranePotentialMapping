/*!
 * \file hoc_command.cpp
 * TODO: NEURON really needs header files for all components (why arent they provided already in NEURON...)
 *
 *  Created on: May 30, 2014
 *      Author: stephan
 */

/*
// extern C functions (NEURON)
extern bool hoc_valid_stmt(const char* stmt, Object* ob);
extern int ivocmain(int, char**, char**);
extern double hoc_ac_;

namespace ug {
	namespace membrane_potential_mapping {
	/////////////////////////////////////////////////////////
		/// execute
	    /////////////////////////////////////////////////////////
		bool HocCommand::execute() const {
			return hoc_valid_stmt(m_command.str().c_str(), 0);
		}

	    /////////////////////////////////////////////////////////
		/// result
	    /////////////////////////////////////////////////////////
		number HocCommand::result() const {
			return hoc_ac_;
		}

	    /////////////////////////////////////////////////////////
		/// clear
	    /////////////////////////////////////////////////////////
		void HocCommand::clear() {
			m_command.clear();
		}
	}
}
*/
